// Shared between create-service-account.ts and cloud-sync-worker.ts so the
// two can never drift apart. DO NOT regenerate this value — see
// create-service-account.ts for why it must stay fixed across every
// database it's ever run against.
export const SYNC_SERVICE_ACCOUNT_ID = "9ba1c590-9ca7-4258-bc93-5e4819b7ef73";
